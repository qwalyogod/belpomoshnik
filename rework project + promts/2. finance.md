Ты работаешь с моим дипломным проектом «Белпомощник».

Задача: доработать раздел «ЖКХ и налоги», чтобы он выглядел не как сырой список, а как реально полезный раздел для пользователя: понятный dashboard, сроки, показания, квитанции, налоговые обязательства, подсказки, demo-данные.

Перед началом прочитай:

* `AGENTS.md`
* `docs/PROJECT_STATUS.md`
* `docs/TASKS.md`
* `docs/API_CONTRACTS.md`
* `docs/DESIGN_SYSTEM.md`
* текущие файлы:

  * `src/backend/models.py`
  * `src/backend/api/trackers.py`
  * `src/backend/migrations/0004_utility_tax.sql`
  * `reactvitemaket/src/app/data/types.ts`
  * `reactvitemaket/src/app/data/adapters.ts`
  * `reactvitemaket/src/app/data/mock.ts`
  * `reactvitemaket/src/app/data/store.tsx`
  * `reactvitemaket/src/app/services/api.ts`
  * `reactvitemaket/src/app/services/reminders.ts`
  * `reactvitemaket/src/app/pages.tsx`

Важное:

* Не делай реальную оплату банковскими картами.
* Не делай интеграцию с ЕРИП как будто она уже работает.
* Не придумывай реальные тарифы, если нет официальной интеграции.
* Это должен быть личный органайзер: пользователь хранит счета, сроки, суммы, квитанции, показания и получает подсказки.
* Все юридически важные тексты формулируй осторожно: “проверьте в личном кабинете/ЕРИП/МНС”, “данные для демонстрации”.
* Интерфейс на русском языке.

Что сейчас плохо:

1. В `FinanceBody` есть bug: налоги в summary читаются как `t.dueDate`, но тип `TaxObligation` использует `deadline`. Исправить на `deadline`.
2. ЖКХ и налоги выглядят как CRUD-карточки без нормального сценария использования.
3. Нет структуры коммунальных услуг: вода, электроэнергия, отопление, газ, капремонт, обслуживание.
4. Нет нормального workflow “передать показания” и “оплатить/отметить оплату”.
5. Нет квитанций/чеков как вложений.
6. Нет связи с адресами пользователя.
7. Налоги не объясняют пользователю, зачем раздел нужен.
8. Демо-данных мало, они не показывают богатый сценарий на защите.

Нужно сделать новый UX раздела.

Раздел `/finance` должен состоять из вкладок или сегментов:

1. “Обзор”
2. “ЖКХ”
3. “Налоги”
4. “Заявки 115”
5. “История”

Можно сделать проще, если не хватает времени: tabs внутри одного `FinancePage`.

1. Обзор.

Добавь сверху dashboard-карточки:

* ближайший срок;
* просрочки;
* сумма к оплате в этом месяце;
* показания, которые нужно передать;
* налоги к оплате;
* последний оплаченный платёж.

Добавь блок “Что сделать сейчас”:

* “Передать показания до …”
* “Оплатить ЖКХ за …”
* “Проверить налоговое извещение”
* “Скачать/прикрепить квитанцию”
* “Создать заявку 115”

Каждый пункт должен быть кликабельным и вести к нужной карточке/форме.

2. ЖКХ.

Расширь модель `UtilityAccount`.

Сейчас есть:

* address
* accountNumber
* provider
* payments

Нужно добавить или предусмотреть:

* `label` — название адреса, например “Квартира”, “Дом родителей”
* `serviceTypes` — массив услуг: electricity, cold_water, hot_water, gas, heating, maintenance, capital_repair, waste
* `payerName` или `ownerName` опционально
* `personalAccountType` / `providerCategory` опционально
* `lastSyncNote` / `manualMode` — чтобы честно показать, что это ручной трекер

Для платежей `UtilityPayment` добавить/поддержать:

* `period`
* `amount`
* `status`
* `paymentDeadline`
* `paymentDate`
* `readingsDeadline`
* `readingsDate`
* `receiptPath` или `receiptMeta`
* `source` = manual / erip / receipt
* `breakdown` — массив начислений по услугам:

  * service
  * amount
  * previousReading
  * currentReading
  * consumption
  * tariffLabel
* `comment`

Если не хочешь усложнять БД сильно, можно хранить `breakdown_json` как Text/JSON-строку.

UI для ЖКХ:

* карточка адреса;
* внутри карточки: поставщик, лицевой счёт, ближайший срок, сумма;
* кнопки:

  * “Передать показания”
  * “Отметить оплачено”
  * “Прикрепить квитанцию”
  * “История”
  * “Заявка 115”
* отдельная форма добавления показаний:

  * холодная вода;
  * горячая вода;
  * электричество;
  * газ;
  * дата передачи.
* показывай разницу расхода: текущее - предыдущее.
* подсветка:

  * зелёный — оплачено;
  * синий — предстоит;
  * жёлтый — скоро срок;
  * красный — просрочено.
* если данных нет — красивое empty state: “Добавьте лицевой счёт, чтобы видеть сроки и напоминания”.

3. Заявки 115.

Добавь простую модель “заявка ЖКХ/115” без реальной отправки:

* id
* user_id
* address/account_id
* title
* category
* description
* status: draft / sent / in_progress / resolved / rejected
* created_at
* updated_at
* target_service
* photo_path или attachment metadata, если быстро реализуемо
* external_number опционально
* external_url опционально

Backend endpoints:

* `GET /api/user/utility/requests`
* `POST /api/user/utility/requests`
* `PUT /api/user/utility/requests/{id}`
* `DELETE /api/user/utility/requests/{id}`

UI:

* вкладка “Заявки 115”;
* кнопка “Создать заявку”;
* категории:

  * нет воды;
  * отопление;
  * электрика;
  * мусор/двор;
  * подъезд;
  * лифт;
  * другое.
* карточки статусов;
* предупреждение: “Это демо-черновик. Для реальной отправки используйте официальный портал/приложение 115.бел”.

4. Налоги.

Исправь `dueDate` -> `deadline`.

Расширь `TaxObligation`:

* `taxType`: property / land / transport / income / single_property_payment / ip / other
* `source`: manual / mns_notice / demo
* `recipient`: ИМНС или “МНС”
* `unp` опционально
* `noticeNumber` опционально
* `paymentCode` опционально
* `receiptPath` / receipt metadata
* `paidAt`
* `externalUrl` опционально
* `helpText` короткое объяснение

Для UI налогов:

* отдельная вкладка “Налоги”;
* карточки:

  * “Единый имущественный платёж”
  * “Налог на недвижимость”
  * “Земельный налог”
  * “Транспортный налог”
  * “Подоходный налог / декларация”
* показывай:

  * сумма;
  * период;
  * срок;
  * статус;
  * источник;
  * подсказка что делать.
* действия:

  * “Отметить оплачено”
  * “Прикрепить чек”
  * “Открыть инструкцию”
  * “Добавить налог вручную”
* блок “Проверить официально”:

  * “Личный кабинет плательщика МНС”
  * “Оплата через ЕРИП”
  * “Найти УНП”
    Эти кнопки могут быть обычными external links, без имитации интеграции.

5. Умные подсказки на основе профиля.

В проекте у пользователя уже есть поля:

* region
* city
* address
* addresses
* owns_property
* has_car
* is_renter
* employment_status
* has_children

Используй их для подсказок:

* если `owns_property=True`, показывать рекомендацию проверить имущественные налоги;
* если `has_car=True`, показывать транспортный налог и авто-документы;
* если `is_renter=True`, показывать договор найма/аренды и коммунальные платежи;
* если есть несколько addresses, предложить создать лицевой счёт для каждого адреса.

6. Demo seed.

Расширь или создай `src/backend/scripts/seed_demo_personal_data.py`.

Скрипт должен создавать для каждого существующего пользователя:

* 1–2 лицевых счёта ЖКХ;
* платежи за последние 4–6 месяцев;
* один оплаченный платёж;
* один ожидающий платёж;
* один просроченный платёж;
* показания по воде/электричеству;
* квитанцию/receipt metadata, если реализована загрузка;
* налог на недвижимость, если `owns_property=True`;
* транспортный налог, если `has_car=True`;
* земельный налог для части пользователей;
* один налог со статусом “Оплачено”;
* один налог со статусом “Предстоит”.

Данные должны быть:

* фейковые;
* детерминированные;
* идемпотентные;
* визуально разнообразные для защиты.

7. Backend.

Обнови:

* `models.py`
* `api/trackers.py`
* migrations
* schemas внутри `api/trackers.py` или отдельные schemas, если проект так устроен
* tests

Все endpoints должны проверять владельца.
Другой пользователь не должен видеть или менять чужие счета, платежи, налоги, заявки.

8. Frontend.

Обнови:

* `types.ts`
* `adapters.ts`
* `store.tsx`
* `api.ts`
* `pages.tsx`
* при необходимости вынеси `FinanceBody` на компоненты, чтобы файл не стал нечитаемым:

  * `FinanceOverview`
  * `UtilityAccountsSection`
  * `TaxesSection`
  * `UtilityRequestsSection`
  * `ReceiptUpload`
  * `StatusPill`

Не делай огромный визуальный хаос. Нужен чистый современный интерфейс:

* крупные карточки;
* понятные статусы;
* минимум таблиц;
* много “что сделать сейчас”;
* хороший empty state;
* мобильная версия не должна ломаться.

9. Напоминания.

Обнови `services/reminders.ts`:

* документы — как было;
* ЖКХ:

  * напоминание о передаче показаний;
  * напоминание об оплате;
  * просрочка;
* налоги:

  * напоминание до срока;
  * просрочка;
* заявки 115:

  * напоминание проверить статус, если долго без изменений.

10. Тесты.

Добавь/обнови:

* `tests/test_trackers.py`
* новый тест для utility requests, если добавлена модель
* тесты owner isolation
* тесты CRUD для расширенных полей
* тест на bug `deadline` в tax summary можно покрыть frontend build/typecheck, если доступно

11. Документация.

Обнови:

* `docs/PROJECT_STATUS.md`
* `docs/CHANGELOG.md`
* `docs/API_CONTRACTS.md`
* `docs/DECISIONS.md`

12. Проверки.

Запусти:

* `PYTHONPATH=src pytest tests/test_trackers.py tests/test_security_fixes.py`
* если есть pnpm: `pnpm -C reactvitemaket run build`

В конце дай отчёт:

* какие файлы изменены;
* какие новые endpoint'ы добавлены;
* какие demo данные создаются;
* что именно пользователь теперь может сделать в разделе ЖКХ и налогов;
* какие проверки прошли;
* что осталось TODO.
