Ты работаешь с моим дипломным проектом «Белпомощник».

Задача: полностью доработать админ-панель так, чтобы она давала администратору полный логический контроль над всем, что есть на сайте и в приложении. Админка должна быть не декоративной, а настоящей системой управления контентом, пользователями, ролями, публикациями, регионами, учреждениями, проблемами, сценариями, уведомлениями и служебными данными.

Перед началом обязательно прочитай:

* `AGENTS.md`
* `docs/PROJECT_STATUS.md`
* `docs/TASKS.md`
* `docs/API_CONTRACTS.md`
* `docs/SECURITY_POLICY.md`
* `docs/EDITOR_PANEL_VISION.md`
* `docs/DEMO_SCRIPT.md`
* `src/backend/models.py`
* `src/backend/api/admin.py`
* `src/backend/api/auth.py`
* `src/backend/api/articles.py`
* `src/backend/api/situations.py`
* `src/backend/api/assistant.py`
* `src/backend/app.py`
* все backend-файлы, где есть admin/moderation/content endpoints
* `reactvitemaket/src/app/components/desktop.tsx`
* `reactvitemaket/src/app/components/content-editor.tsx`
* `reactvitemaket/src/app/services/api.ts`
* `reactvitemaket/src/app/data/types.ts`
* `reactvitemaket/src/app/data/store.tsx`
* `reactvitemaket/src/app/pages.tsx`
* все компоненты админ-панели

Важное:

* Не переписывай проект с нуля.
* Не меняй стек.
* Backend остаётся FastAPI + SQLAlchemy + MySQL.
* Frontend остаётся React + TypeScript + Vite.
* Не ломай публичную часть сайта.
* Все действия администратора должны проверяться на backend, а не только скрываться на frontend.
* Обычный пользователь не должен иметь доступ к admin endpoints.
* Редактор не должен иметь доступ к управлению пользователями, ролями, банами и системными настройками.
* Админ должен видеть и редактировать всё, что реально отображается на сайте.
* Если какая-то сущность есть в базе и на сайте, но её нет в админке, нужно добавить её в админку.

Главная цель:
Админ-панель должна стать единым центром управления проектом. В ней должны быть видны все основные сущности сайта:

* публикации/новости;
* правовые обновления;
* проблемы;
* жизненные ситуации/сценарии;
* категории;
* теги;
* учреждения;
* регионы и города;
* экстремистский реестр/материалы;
* пользователи;
* роли;
* предложения публикаций;
* модерация;
* уведомления;
* журнал действий;
* системные настройки;
* AI-помощник редактора, если он уже есть.

Сейчас есть проблема:

1. Новости и экстремистский контент уже частично поправлены.
2. Учреждения начали отображаться лучше.
3. Но проблемы и ситуации, которые добавлены в программу, до сих пор не видны в админке.
4. Нет полноценного управления всеми пользователями.
5. Нет понятной системы ролей.
6. Нет нормальной логики банов/блокировок.
7. Нет полноценного управления предложениями публикаций.
8. Нет единой логики публикации/черновиков/архива.
9. Нет уверенности, что админ видит всё, что есть на сайте.

Нужно сделать структуру админ-панели.

Разделы бокового меню должны быть логически такими:

1. Обзор

* статистика по сайту:

  * всего пользователей;
  * активных пользователей;
  * заблокированных пользователей;
  * публикаций;
  * проблем;
  * сценариев;
  * учреждений;
  * регионов;
  * материалов на модерации;
  * жалоб/предложений, если есть;
  * уведомлений;
* быстрые действия:

  * создать публикацию;
  * создать проблему;
  * создать сценарий;
  * добавить учреждение;
  * открыть модерацию;
  * открыть пользователей.

2. Публикации

* новости;
* статьи;
* правовые обновления;
* статусы:

  * черновик;
  * на модерации;
  * опубликовано;
  * архив;
  * отклонено.
* действия:

  * создать;
  * редактировать;
  * опубликовать;
  * снять с публикации;
  * отправить в архив;
  * удалить;
  * посмотреть на сайте.
* фильтры:

  * статус;
  * категория;
  * автор;
  * дата;
  * регион;
  * поиск.

3. Проблемы
   Добавить полноценный список всех проблем из базы.

Для каждой проблемы показывать:

* название;
* slug/id;
* статус публикации;
* категория;
* региональная привязка, если есть;
* связанные сценарии;
* дата создания/обновления;
* автор/редактор;
* видна ли на сайте.

Действия:

* создать проблему;
* редактировать проблему;
* привязать сценарии;
* привязать учреждения;
* привязать документы;
* опубликовать;
* снять с публикации;
* архивировать;
* удалить;
* открыть публичную карточку.

Важно:
если проблема отображается на сайте, она обязательно должна быть видна в админке.
если проблема есть в базе, но не опубликована, она тоже должна быть видна в админке с соответствующим статусом.

4. Сценарии
   Добавить полноценный список всех жизненных сценариев.

Для каждого сценария показывать:

* название;
* id/slug;
* статус;
* категория;
* связанная проблема;
* количество этапов;
* количество шагов;
* документы;
* учреждения;
* сроки;
* дата обновления;
* видимость на сайте.

Действия:

* создать сценарий;
* редактировать;
* редактировать этапы;
* редактировать шаги;
* привязать документы;
* привязать учреждения;
* привязать источники;
* опубликовать;
* снять с публикации;
* архивировать;
* удалить;
* открыть публичную страницу;
* проверить целостность сценария.

Добавить проверку целостности:

* есть ли название;
* есть ли описание;
* есть ли хотя бы один этап;
* есть ли шаги;
* есть ли связанные документы, если они нужны;
* есть ли учреждения, если сценарий требует обращения;
* нет ли битых ссылок;
* нет ли пустых обязательных полей.

5. Учреждения
   Список учреждений должен быть полноценным.

Показывать:

* название;
* тип учреждения;
* регион;
* город/район;
* адрес;
* телефон;
* сайт/email, если есть;
* связанные проблемы;
* связанные сценарии;
* статус актуальности;
* дата обновления.

Действия:

* создать;
* редактировать;
* привязать к региону;
* привязать к сценариям;
* привязать к проблемам;
* скрыть;
* удалить;
* открыть карточку/место на сайте.

Если отдельной публичной страницы учреждения нет — не делать битые ссылки. Либо вести в каталог с фильтром, либо добавить нормальный route.

6. Категории и теги
   Нужен полный CRUD:

* создать;
* редактировать;
* удалить;
* объединить дубликаты;
* посмотреть связанные материалы;
* запретить удаление, если категория используется, либо предложить переназначение.

7. Регионы и города
   Этот раздел будет дорабатываться отдельным промптом, но здесь важно:

* регионы должны сохраняться стабильно;
* после переименования регион не должен пропадать с карты;
* после создания новый регион должен быть виден в списке и на карте;
* позиции карточек на карте должны сохраняться;
* районы/города должны редактироваться;
* должна быть возможность открыть список регионов сбоку на desktop/tablet.

8. Экстремистский реестр
   Сохранить уже исправленное отображение, но добавить полный контроль:

* список записей;
* поиск;
* фильтры;
* статус;
* источник;
* дата включения;
* дата обновления;
* тип материала;
* редактор;
* действия:

  * создать;
  * редактировать;
  * снять с публикации;
  * архивировать;
  * удалить.
* Везде добавить предупреждение, что юридически значимые данные требуют проверки официального источника.

9. Пользователи и роли
   Добавить полноценный раздел управления пользователями.

Список пользователей:

* id;
* email;
* имя;
* роль;
* статус;
* дата регистрации;
* последний вход;
* количество личных ситуаций;
* количество документов;
* количество уведомлений;
* флаги:

  * активен;
  * заблокирован;
  * email подтверждён;
  * демо-пользователь.

Фильтры:

* роль;
* статус;
* дата регистрации;
* поиск по email/name/id;
* заблокированные;
* администраторы;
* редакторы;
* обычные пользователи.

Действия администратора:

* открыть карточку пользователя;
* изменить роль;
* заблокировать;
* разблокировать;
* удалить пользователя;
* сбросить пароль или отправить reset link, если такая логика есть;
* принудительно завершить сессии, если в проекте есть сессии/token blacklist;
* посмотреть журнал действий пользователя;
* посмотреть связанные сущности пользователя;
* создать системное уведомление пользователю.

Роли:

* citizen / user — обычный пользователь;
* content_editor / editor — редактор;
* moderator — модератор, если роль есть;
* admin / platform_admin — администратор.

Важно:

* нельзя позволять администратору случайно удалить самого себя;
* нельзя позволять редактору назначать роли;
* нельзя позволять обычному пользователю попасть в admin API;
* при удалении пользователя нужно либо soft delete, либо понятное подтверждение с последствиями.
* Лучше реализовать soft delete/deactivate, а физическое удаление только если уже есть безопасная логика.

Карточка пользователя:

* основная информация;
* роль и статус;
* действия;
* личные ситуации;
* документы;
* уведомления;
* активность;
* журнал изменений.

10. Модерация и предложения публикаций
    Нужно логически продумать, что происходит с предложениями.

Сущности:

* предложенная новость;
* предложенное изменение;
* предложенная проблема/сценарий, если есть;
* жалоба на контент, если есть;
* комментарий модератора.

Статусы:

* новое;
* на рассмотрении;
* принято;
* отклонено;
* требует правок;
* опубликовано;
* архив.

Действия:

* открыть предложение;
* принять;
* отклонить;
* вернуть на доработку;
* преобразовать в публикацию;
* связать с существующей публикацией;
* оставить комментарий;
* заблокировать пользователя за спам, если нужно.

Запреты:

* если пользователь заблокирован, он не должен создавать предложения публикаций;
* если пользователь удалён/деактивирован, его предложения должны быть видны администратору, но новые создавать нельзя;
* редактор может модерировать только контент, если ему разрешено;
* только админ может менять глобальные правила.

11. Журнал действий
    Добавить или доработать audit log.

Логировать:

* вход в админку;
* создание сущности;
* редактирование;
* удаление;
* публикацию;
* снятие с публикации;
* изменение роли;
* блокировку пользователя;
* разблокировку;
* удаление пользователя;
* изменение регионов;
* изменение учреждений;
* действия с экстремистским реестром;
* действия AI-помощника редактора, если применимо.

Поля:

* actor_user_id;
* action;
* entity_type;
* entity_id;
* before_json;
* after_json;
* ip/user_agent, если уже есть доступ;
* created_at.

В UI:

* список событий;
* фильтры по пользователю;
* фильтры по сущности;
* фильтры по действию;
* дата;
* просмотр diff, если быстро возможно.

12. Backend API
    Проверь существующие endpoints и добавь недостающие.

Нужны endpoints для:

* admin dashboard stats;
* admin users CRUD/status/roles;
* admin problems CRUD/list;
* admin scenarios CRUD/list;
* admin authorities CRUD/list;
* admin categories/tags CRUD/list;
* admin regions CRUD/list;
* admin moderation queue;
* admin audit log;
* admin publication workflow.

Все endpoints:

* должны требовать JWT;
* должны проверять роль;
* должны возвращать понятные ошибки;
* должны поддерживать pagination;
* должны поддерживать search/filter/sort;
* не должны возвращать лишние чувствительные данные обычным редакторам.

13. Frontend architecture
    Не складывай всю админку в один огромный компонент.

Если сейчас админка находится в одном большом файле, разнеси аккуратно:

* `components/admin/AdminLayout.tsx`
* `components/admin/AdminSidebar.tsx`
* `components/admin/AdminDashboard.tsx`
* `components/admin/AdminPublications.tsx`
* `components/admin/AdminProblems.tsx`
* `components/admin/AdminScenarios.tsx`
* `components/admin/AdminAuthorities.tsx`
* `components/admin/AdminUsers.tsx`
* `components/admin/AdminRoles.tsx`
* `components/admin/AdminModeration.tsx`
* `components/admin/AdminRegions.tsx`
* `components/admin/AdminAuditLog.tsx`
* `components/admin/AdminEmptyState.tsx`
* `components/admin/AdminDataTable.tsx`
* `components/admin/AdminStatusBadge.tsx`
* `components/admin/AdminConfirmDialog.tsx`

Если проект уже имеет другую структуру — используй её, но не делай нечитаемый монолит.

14. UI/UX требования
    Админка должна быть удобной:

Desktop:

* полноценная таблица;
* фильтры сверху или сбоку;
* bulk actions;
* правый inspector/drawer для выбранной записи;
* не открывать огромные модалки там, где лучше боковая панель.

Tablet:

* таблицы превращаются в карточки или compact-list;
* inspector открывается как drawer;
* sidebar может сворачиваться.

Mobile:

* оставить текущую хорошую мобильную логику;
* использовать bottom sheet/fullscreen modal;
* кнопки крупные;
* без горизонтального скролла, кроме таблиц, где он действительно нужен.

15. Единая логика статусов
    Сделай одинаковые статусы по всей админке:

* Черновик
* На модерации
* Опубликовано
* Скрыто
* Архив
* Отклонено
* Требует правок
* Заблокирован
* Активен

Не показывай сырые значения вроде `published`, `draft`, `is_active` без перевода.

16. Безопасные destructive actions
    Для удаления/бана/снятия с публикации:

* confirmation dialog;
* объяснение последствий;
* для удаления пользователя — дополнительное подтверждение;
* для удаления региона/категории — проверка связанных сущностей;
* если сущность используется, предложить архивировать вместо удаления.

17. Тесты
    Добавь/обнови backend tests:

* admin видит список пользователей;
* editor не может менять роли;
* citizen не имеет доступа к admin endpoints;
* admin может назначить роль;
* admin может заблокировать/разблокировать пользователя;
* admin не может случайно удалить самого себя;
* problems видны в админке;
* scenarios видны в админке;
* admin может менять статус публикации;
* audit log создаётся при важных действиях;
* заблокированный пользователь не может создавать предложения публикаций;
* pagination/filter работают.

Файлы:

* `tests/test_admin.py`
* `tests/test_admin_users.py`
* `tests/test_admin_content.py`
* `tests/test_security_fixes.py`

Frontend:

* если есть тесты — добавить smoke tests;
* обязательно добиться `pnpm -C reactvitemaket run build`.

18. Документация
    Обнови:

* `docs/API_CONTRACTS.md`
* `docs/PROJECT_STATUS.md`
* `docs/CHANGELOG.md`
* `docs/SECURITY_POLICY.md`
* `docs/EDITOR_PANEL_VISION.md`
* `docs/DEMO_SCRIPT.md`

В `docs/DEMO_SCRIPT.md` добавить сценарий показа админки:

1. Войти как admin.

2. Открыть обзор.

3. Показать статистику.

4. Открыть пользователей.

5. Изменить роль тестового пользователя.

6. Заблокировать/разблокировать тестового пользователя.

7. Открыть проблемы и показать, что они есть в админке.

8. Открыть сценарии и показать этапы/шаги.

9. Открыть учреждения.

10. Открыть регионы.

11. Открыть модерацию.

12. Открыть журнал действий.

13. Проверки после работы
    Запусти:

* `PYTHONPATH=src pytest tests/test_admin.py tests/test_admin_users.py tests/test_admin_content.py tests/test_security_fixes.py`
* если есть pnpm: `pnpm -C reactvitemaket run build`

В конце дай отчёт:

* какие разделы админки добавлены;
* какие backend endpoints добавлены;
* какие роли и ограничения работают;
* как теперь отображаются проблемы и сценарии;
* как работает управление пользователями;
* как работает бан/разбан;
* как работает журнал действий;
* какие тесты прошли;
* что осталось TODO.
