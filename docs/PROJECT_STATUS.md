# Текущее состояние проекта

Дата создания: 2026-05-13.
Дата последнего обновления: 2026-06-04 (React/Vite backend bridge для ситуаций и задач).

Версия: First Beta / первая бета.

## Текущий курс UI

- Рабочий baseline проекта остаётся на Flet/Python.
- Папка `reactvitemaket/` принята как frontend-кандидат для постепенного переноса UI на Vite/React/TypeScript.
- Backend FastAPI + SQLite MVP сохраняется и должен стать источником данных для React frontend через API.
- Flet UI не удаляется и не ломается до достижения функционального parity в React.
- Созданы документы:
  - `docs/REACT_MIGRATION_AUDIT.md`;
  - `docs/REACT_MIGRATION_PLAN.md`.
- `reactvitemaket/package.json` сокращён до реально используемых зависимостей, чтобы первый frontend запуск не тянул неиспользуемый Radix/MUI/Charts-набор.
- Зависимости `reactvitemaket/` установлены через pnpm, создан `pnpm-lock.yaml`.
- `pnpm build` для React/Vite-макета проходит без ошибок.
- React/Vite dev-сервер проверен на `http://127.0.0.1:8560/`: главная страница открывается без runtime traceback.
- Добавлены маршруты React `/scenarios` и `/admin`; `/admin` закрыт role guard для не-администратора.
- Создана начальная frontend-основа для API-интеграции: `reactvitemaket/src/app/services/api.ts` и `reactvitemaket/src/app/data/adapters.ts`.
- React/Vite store подключен к публичным backend endpoints `/api/problems` и `/api/scenarios` с mock fallback.
- Детальная страница React-сценария умеет догружать полный сценарий через `/api/scenarios/{slug}`.
- React/Vite store подключен к публичным endpoints `/api/documents`, `/api/authorities` и `/api/law-updates`.
- Глобальный поиск React-версии теперь видит справочные документы и учреждения из backend API.
- Для публичного React-контента выбрана стратегия `API-first`: backend-данные заменяют одноимённые mock-карточки, mock остаётся fallback при недоступном API.
- Backend summary DTO сценариев расширен полями `stage_count` и `task_count`; React-каталог показывает число задач из API, а mock-массивы остаются только fallback для подробного preview.
- В React/Vite добавлена локальная основа аккаунтов: гость, гражданин, редактор, администратор, быстрый вход, регистрация нового локального пользователя, выход в гостевой режим и protected actions для личных действий.
- В React/Vite добавлено локальное per-user хранение личных данных: ситуации, задачи, документы, избранное, уведомления, профиль и настройки сохраняются отдельно для каждого аккаунта.
- Гостевой режим в React/Vite получает пустую личную область и не может создавать ситуации, отмечать задачи или добавлять документы.
- В React/Vite добавлен мягкий backend auth bridge: тестовые и локально зарегистрированные аккаунты пытаются получить JWT через `/api/auth/login` или `/api/auth/register`, при недоступном backend сохраняется локальный режим.
- Личные документы React/Vite подключены к backend endpoint `/api/user/documents`: список документов догружается из API, создание/редактирование/удаление backend-документов синхронизируются с API, локальный fallback остаётся рабочим.
- Пользовательские ситуации и задачи React/Vite подключены к backend endpoints `/api/user/situations` и `/api/user/tasks`: после JWT-входа ситуации догружаются из API, создание ситуации из сценария создаёт backend-ситуацию с задачами, отметка задачи синхронизируется через API, локальный fallback остаётся рабочим.
- В backend добавлен публичный endpoint `GET /api/scenarios`, CORS для React dev-сервера и категория в DTO полного сценария.
- Локальная backend-база обновлена миграциями `0003`-`0006` и заполнена MVP seed-данными сценария «Рождение ребёнка».
- Следующий маленький шаг: подключать профиль, настройки и уведомления React-версии к backend API, сохраняя текущий локальный fallback.

## Что уже сделано

- Создана рабочая папка проекта `belpomoshnik`.
- Создано виртуальное окружение `.venv`.
- Установлен Flet 0.85.0.
- Создана базовая структура Flet-приложения.
- Созданы экраны: Главная, Проблемы, Карточка проблемы, Мои ситуации, Документы, Уведомления, Профиль.
- Добавлены моковые данные.
- Добавлены базовые компоненты: верхняя панель, нижняя навигация, карточки, кнопки.
- Созданы `README.md`, `SETUP_REPORT.md`, `requirements.txt`, `pyproject.toml`, `.gitignore`.
- Создана документационная основа проекта в `docs/`.
- Создан `AGENTS.md` с правилами для AI-агентов.
- React/Vite-макет из `Web app design for Belpomoshch` использован как визуальный reference.
- Дизайн основных экранов переписан на Flet/Python без JSX, CSS, WebView и npm-зависимостей.
- Добавлены Flet-экраны `LegalUpdatesPage`, `LawDetailPage`, `SituationDetailPage` и моковый `AdminPage`.
- Навигация переведена на маршруты Flet через `on_route_change` и `push_route`.
- Моковые данные расширены категориями, проблемами, документами, уведомлениями, закон-апдейтами и задачами ситуации.
- Исправлен пустой стартовый экран: первый рендер теперь выполняется сразу, без ожидания события смены route.
- Установлены и зафиксированы Flet runtime-пакеты `flet-cli`, `flet-desktop` и `flet-web` версии 0.85.0.
- Исправлен запуск в браузере: `flet-web` больше не устанавливается во время проверки внешнего вида.
- Основные экраны визуально доработаны по React/Vite reference-скриншотам: верхняя навигация, футер, светлый фон, карточки, чипы, профиль, документы, ситуации, каталог, главная и закон-апдейты.
- На мобильном размере добавлена кастомная нижняя навигация Flet без переноса подписей.
- Исправлены визуальные проблемы: широкий поиск в каталоге, корректные статусы карточек документов, фото профиля без наложения заглушки.
- Добавлены маршруты `/problem-detail`, `/situation-detail`, `/legal-updates`, `/law-detail`, `/learning` с поддержкой старых маршрутов `/problem/<id>`, `/situations/<id>`, `/laws/<id>`.
- Добавлены поиск и фильтрация каталога проблем по моковым данным.
- В карточке проблемы добавлены интерактивные чекбоксы шагов и прогресс выполнения в текущей сессии.
- Добавлен вспомогательный обучающий микро-модуль: переключатель в профиле, блок «Проверьте себя», микро-тест по теме «Потерял паспорт», экран `LearningProgressPage`.
- В профиле добавлен блок достижений и переход на экран обучения.
- Исправлено падение на iPhone при переключении вкладок: убрана проверка `isinstance(..., ft.ControlEvent)`, несовместимая с Python 3.14.
- Доработана маршрутизация нижней навигации с безопасной обработкой строки/события.
- Выровнен desktop-визуал экранов `NotificationsPage`, `SituationDetailPage` и `AdminPage`: добавлены единые контейнеры ширины и отступов.
- Обновлена базовая цветовая подложка интерфейса до `#F6F8FB` согласно дизайн-системе.
- Добавлена корректная mobile safe-area адаптация для iPhone с челкой: контент больше не уходит под верхние системные элементы.
- Нижняя навигация обернута в безопасную область по нижнему краю, чтобы не конфликтовать с системной gesture-зоной.
- Уменьшен лишний нижний отступ контента, из-за которого появлялась визуальная «серая полоса» над нижней навигацией.
- Добавлена backend-основа для админ-панели и сценариев:
  - `src/backend/models.py` с сущностями Problem, Scenario, Stage, Step, Document, Authority, Deadline, NotificationRule, Dependency, RelatedScenario, SourceReference, LawUpdate;
  - `src/backend/migrations/0001_initial.sql`;
  - public API и admin API endpoints в `src/backend/api`;
  - сервисный слой `src/backend/service.py`;
  - seed-данные MVP-сценария «Рождение ребёнка» в `src/backend/seeds/mvp_childbirth.py`.
- Добавлены backend-скрипты:
  - `PYTHONPATH=src python -m backend.scripts.init_db`;
  - `PYTHONPATH=src python -m backend.scripts.seed_db`.
- Доработана Flet `AdminPage` под новые разделы админ-панели (проблемы, сценарии, конструктор, документы, организации, источники, law updates).
- Flet `AdminPage` подключена к backend API:
  - загрузка проблем и сценариев из `/api/admin/problems` и `/api/admin/scenarios`;
  - создание проблемы из UI;
  - создание сценария из UI;
  - публикация/возврат в draft для проблем и сценариев;
  - отображение статуса подключения API и времени последней синхронизации.
- Доработан backend API для конструктора сценариев:
  - `GET /api/admin/scenarios/{id}`;
  - `GET /api/admin/scenarios/{id}/stages`;
  - `GET /api/admin/stages/{id}/steps`.
- Доработана Flet `AdminPage` как рабочий конструктор сценариев:
  - выбор сценария для редактирования;
  - формы добавления этапов и шагов;
  - выбор action type, срока, организации и документов для шага;
  - визуальный список этапов и шагов выбранного сценария;
  - переключение обязательности для этапов и шагов;
  - синхронизация конструктора через API без перезапуска UI.
- Добавлена интерактивная фильтрация раздела «Изменения законодательства»:
  - поиск по заголовку/описанию/целевой аудитории;
  - чипы категорий с реальной фильтрацией;
  - аккуратное пустое состояние при отсутствии совпадений.
- Начат поэтапный переход мобильного UI на API:
  - для карточки проблемы «Рождение ребёнка» данные теперь могут подтягиваться из public API-сценария `rozhdenie-rebenka`;
  - при недоступном API автоматически используется моковый fallback без падений.
- Добавлена MVP-авторизация и регистрация на Flet:
  - маршруты `/login` и `/register`;
  - демо-вход `ivan@example.by / 123456`;
  - создание локального аккаунта с сохранением в `data/app_state.json`;
  - выход из профиля.
- Оживлены основные пользовательские кнопки:
  - создание персонального плана из карточки проблемы;
  - сохранение карточки проблемы;
  - добавление документа через форму;
  - добавление ситуации через форму;
  - добавление задачи в детальной ситуации;
  - отметка задач выполненными;
  - сохранение прогресса ситуации;
  - прочтение одного уведомления и всех уведомлений;
  - сохранение профиля;
  - добавление интереса;
  - сохранение закон-апдейта и открытие официального URL первоисточника.
- Расширены статьи/карточки проблем в `src/data/mock_data.py`: медкнижка, квитанция ЖКХ, налоговая ошибка, переезд, открытие ИП, увольнение.
- Добавлено локальное сохранение состояния в `data/app_state.json`:
  - демо-пользователи и локальная регистрация;
  - профиль и настройки;
  - добавленные документы, ситуации и задачи;
  - прочитанные уведомления;
  - сохранённые карточки проблем и закон-апдейты.
- Доработан глобальный поиск:
  - фильтр «Рядом» вынесен в отдельный режим;
  - быстрый фильтр «Рядом со мной» использует адреса из профиля пользователя;
  - подбор учреждений учитывает все сохранённые адреса профиля, выбирает лучшее совпадение и убирает дубли;
  - добавлена кнопка сброса поиска и фильтра;
  - исправлен серый web-плейсхолдер пустого блока поиска.
- Доработан раздел «Документы»:
  - у выбранного документа появились явные действия «Открыть скан», «Редактировать», «Удалить»;
  - кнопка открытия скана теперь передаёт выбранный документ, а не событие клика;
  - форма документа использует календарь для даты выдачи и срока действия вместо ручного ввода;
  - добавлена фиксированная палитра цветов карточек документа;
  - импорт PDF сохраняет цвет документа по умолчанию;
  - формулировки защиты документов приведены к фактической локальной модели хранения без обещания биометрии.
- Задачи в детальной ситуации теперь привязаны к конкретной ситуации, чтобы прогресс не смешивался между планами.
- Кнопка «Открыть первоисточник» в деталях закон-апдейта теперь открывает официальный URL из моковых данных.
- Расширены карточки проблем: «Рождение ребёнка», «Расторжение брака», «Оформление больничного», «Регистрация автомобиля».
- Добавлено локальное редактирование и удаление пользовательских данных:
  - документы можно редактировать и удалять;
  - ситуации можно редактировать и удалять;
  - задачи внутри ситуации можно редактировать и удалять;
  - удаление ситуации очищает связанные локальные задачи;
  - все изменения сохраняются в `data/app_state.json`.
- Добавлены шаблоны жизненных сценариев:
  - новая сущность `SCENARIO_TEMPLATES` в `src/data/mock_data.py`;
  - тестовый шаблон «Рождение ребёнка» с этапами, задачами, документами, организациями и источниками;
  - новый экран `/scenarios` со списком готовых сценариев;
  - новый экран `/scenario-detail` с детальным просмотром шаблона;
  - кнопка «Создать мою ситуацию» создает локальную ситуацию из шаблона и добавляет задачи;
  - задачи из шаблона привязаны к конкретной ситуации и содержат нужные документы;
  - прогресс ситуации автоматически считается по выполненным задачам.
- Расширен каталог готовых жизненных сценариев:
  - «Потеря паспорта»;
  - «Расторжение брака»;
  - «Открытие ИП»;
  - «Переезд и регистрация».
- Для новых сценариев добавлены этапы, задачи, документы, организации и ссылки на официальные источники.
- В разделе «Жизненные сценарии» добавлены поиск и фильтрация по категориям.
- В детальной странице ситуации вместо заглушки добавлен блок «Документы к подготовке», который собирает уникальные документы из задач ситуации.
- В детальной странице ситуации добавлена подпись прогресса «Выполнено X из Y задач».
- В формах добавления и редактирования задач добавлено необязательное поле «Дата срока».
- Ручные задачи теперь могут хранить `due_date` в формате `YYYY-MM-DD`, поэтому они попадают в блоки ближайших и просроченных задач на главной странице.
- Детальная ситуация показывает дату срока рядом с текстовым сроком задачи.
- В формах добавления и редактирования задач добавлено поле «Документы для задачи».
- Документы ручной задачи сохраняются в локальном состоянии и отображаются в детальной ситуации.
- Документы из ручных задач попадают в блок «Документы к подготовке» на главной странице.
- Уведомления теперь синхронизируются с задачами, у которых есть дата срока.
- Для задач с дедлайном на ближайшие 7 дней или с просрочкой создаются локальные уведомления.
- Уведомления по задачам сохраняют статус прочтения и исчезают после выполнения или удаления задачи.
- Блок «Напоминания» на главной странице использует текущие уведомления приложения.
- Усилено локальное хранилище:
  - перед сохранением создается `data/app_state.backup.json`;
  - при повреждении `data/app_state.json` приложение пытается восстановить данные из backup;
  - структура состояния сверяется с дефолтной схемой при запуске.
- Главная страница доработана в пользовательский дашборд:
  - показывает количество активных и завершённых ситуаций;
  - считает общий прогресс по активным ситуациям;
  - показывает 2-3 активные ситуации с прогрессом и количеством задач;
  - собирает ближайшие задачи из всех локальных ситуаций;
  - отдельно показывает просроченные задачи;
  - собирает уникальные документы к подготовке из задач активных ситуаций;
  - добавлены быстрые переходы к «Жизненным сценариям» и «Моим ситуациям».
- Добавлен сервис `src/services/dashboard.py` для расчёта данных главного экрана.
- Сроки задач теперь могут храниться как `due_date` в формате `YYYY-MM-DD`; шаблонные задачи получают дату автоматически при создании ситуации.
- Выполнена iPhone-адаптация layout:
  - добавлены константы ширин 375/393/430 px и безопасных отступов;
  - добавлены общие helpers `app_safe_area`, `mobile_page_layout`, `scrollable_mobile_page`, `iphone_page_container`, `bottom_nav_safe_area`;
  - мобильный контент теперь оборачивается в `ft.SafeArea` с fallback-отступом сверху для notch/Dynamic Island;
  - нижняя навигация защищена от home indicator через отдельный SafeArea;
  - нижняя навигация приведена к актуальным разделам: Главная, Сценарии, Ситуации, Уведомления, Профиль;
  - мобильные диалоги получили адаптивную ширину и прокрутку содержимого;
  - исправлены риски горизонтального overflow в заголовках уведомлений, ситуаций, деталей ситуации и админ-панели;
  - исправлен параметр `Dropdown(on_change=...)` в админке на совместимый с Flet 0.85 `on_select`.
- Проверены через smoke-check: `HomePage`, `SituationsPage`, `SituationDetailPage`, `NotificationsPage`, `ProblemDetailPage`, `AdminPage`, компоненты SafeArea layout и нижняя навигация.
- Проект собран в демонстрационную первую бету для показа научному руководителю:
  - главный пользовательский путь проходит от выбора сценария до личной ситуации и прогресса;
  - основной сценарий «Рождение ребёнка» расширен до 7 этапов;
  - детальная страница сценария показывает задачи, документы, организации, источники и связанные сценарии;
  - при повторном создании уже активного сценария приложение предупреждает о дубле;
  - в «Моих ситуациях» добавлены фильтры «Все», «Активные», «Завершённые» и счётчик задач;
  - в профиле есть сброс демо-данных для повторного показа;
  - добавлена документация `docs/BETA_DEMO_GUIDE.md` и `docs/BETA_ACCEPTANCE_CHECKLIST.md`.
- Проверка первой беты:
  - `.venv/bin/python -m compileall src` выполнена без ошибок;
  - smoke-check ключевых страниц выполнен без ошибок;
  - `.venv/bin/flet run src/main.py` стартует без traceback;
  - `.venv/bin/flet run --web src/main.py` стартует без traceback.
- Выполнена полировка первой беты:
  - добавлен экран `/about`;
  - добавлен onboarding первого запуска;
  - добавлены избранные сценарии с локальным сохранением;
  - улучшены пустые состояния документов, уведомлений, ситуаций и dashboard-блоков;
  - добавлены ненавязчивые подсказки на главной, в сценарии и в ситуации;
  - улучшен блок официальных источников в деталях сценария;
  - выровнены мобильные отступы блока настроек профиля;
  - созданы `docs/DEMO_SCRIPT.md` и `docs/BETA_CHECKLIST.md`.
  - `.venv/bin/python -m compileall src` выполнена без ошибок;
  - `.venv/bin/flet run src/main.py` стартует без traceback.
- Зафиксирована дорожная карта полного закрытия ТЗ после First Beta:
  - создан `docs/TZ_COMPLETION_ROADMAP.md`;
  - `AGENTS.md` теперь требует читать roadmap перед крупными задачами по ТЗ;
  - в `docs/TASKS.md` добавлены этапы: личные документы, зависимости шагов, госучреждения, роли/админка, персонализация, закон-апдейты, уведомления/email, ЖКХ/налоги, backend/PostgreSQL.
- Начат спринт «Личные документы и демонстрационная безопасность»:
  - локальное состояние расширено ключами `personal_documents`, `privacy_settings`, `uploaded_files`;
  - раздел «Мои документы» поддерживает типы документов, серию/номер, даты выдачи и действия, организацию выдачи, комментарий и путь к скану;
  - статус документа автоматически считается по сроку действия: «Активен», «Истекает скоро», «Истёк»;
  - номера документов маскируются по умолчанию;
  - добавлен переключатель «Скрывать личные данные» и кнопка временного показа реквизитов;
  - перед просмотром скана показывается предупреждение о личных данных;
  - добавлен выбор файла через Flet FilePicker;
  - выбранный файл копируется в `data/private_uploads/`, если Flet отдаёт локальный путь;
  - метаданные выбранных файлов сохраняются в `uploaded_files`;
  - создана локальная папка `data/private_uploads/`, она исключена из Git вместе с локальными JSON-файлами состояния.
- Выполнен локальный MVP спринта «Зависимости шагов»:
  - в шаблоны сценариев добавлено поле `depends_on`;
  - при создании ситуации зависимости переводятся из ID шаблонных задач в ID локальных задач;
  - для уже созданных ситуаций зависимости нормализуются при запуске, если они были созданы из шаблона;
  - заблокированные задачи показывают причину блокировки;
  - заблокированную задачу нельзя отметить выполненной до выполнения предыдущих задач;
  - детальная ситуация группирует задачи по этапам;
  - добавлены фильтры задач: все, доступные, заблокированные, выполненные, просроченные.
- Выполнен локальный MVP спринта «Госучреждения»:
  - добавлены `INSTITUTION_TYPES` и `INSTITUTIONS` в `src/data/mock_data.py`;
  - добавлен сервис `src/services/institutions.py` для подбора учреждений по типу, региону и городу пользователя;
  - локальное состояние расширено справочником `institutions`;
  - задачи сценариев связаны с типами учреждений через `institution_types`;
  - при создании ситуации типы учреждений переносятся в локальные задачи;
  - детальная ситуация показывает блок «Куда обращаться» с подобранными учреждениями, адресом, телефоном и сайтом;
  - детальная страница сценария показывает, к каким типам учреждений относится шаг.
- Начат спринт «Роли и админ-панель»:
  - в моковые данные добавлены роли `Гражданин`, `Редактор контента`, `Админ площадки`;
  - локальное состояние расширено ключами `admin_roles`, `admin_users`, `admin_audit_logs`, `current_admin_role`;
  - в админ-панели добавлен блок «Роли и доступ»;
  - админ-панель показывает доступные разделы и права выбранной роли;
  - для роли «Гражданин» админ-действия скрываются как недоступные;
  - для роли «Админ площадки» показываются демо-пользователи и последние действия аудита;
  - настоящий backend RBAC и полноценный аудит остаются отдельным следующим этапом.
- В рамках спринта «Роли и админ-панель» начат CRUD закон-апдейтов для редактора:
  - локальное состояние расширено ключами `law_updates` и `law_detail`;
  - лента «Изменения законодательства» теперь берёт опубликованные записи из локального состояния;
  - детальная страница закон-апдейта использует локальные данные и не привязана жёстко к моковой константе;
  - в админ-панели добавлена форма создания закон-апдейта;
  - добавлены поля: категория, приоритет, кого касается, дата, краткое описание, что сделать пользователю, связанные сценарии и официальный источник;
  - редактор может создать запись, отредактировать все основные поля, перевести запись в `published`/`draft` и удалить локальную демо-запись;
  - действия редактора записываются в демонстрационный аудит;
  - лента закон-апдейтов получила блок «Важное для вас» на основе интересов профиля, активных ситуаций, избранных сценариев и приоритета записи;
  - в форме создания и списке админки добавлена связь закон-апдейтов с проблемами;
  - полноценный backend CRUD закон-апдейтов остаётся следующим production-этапом.
- В рамках спринта «Роли и админ-панель» добавлен локальный CRUD официальных источников:
  - локальное состояние расширено ключом `official_sources`;
  - в `src/data/mock_data.py` добавлен справочник официальных источников: `pravo.by`, `portal.gov.by`, `mintrud.gov.by`, `nalog.gov.by`, `minjust.gov.by`;
  - в админ-панели добавлены форма добавления и список источников;
  - редактор может создать, отредактировать, удалить источник и переключить статус `active` / `requires_check`;
  - URL источника проверяется на официальный белорусский ресурс (`pravo.by` или домен `.gov.by`);
  - действия с источниками записываются в демонстрационный аудит;
  - связь источников с конкретными этапами/шагами и backend CRUD остаются следующими задачами.
- Начат приоритет 1 из отчёта по ТЗ без проверки официальных источников:
  - добавлены отдельные локальные настройки напоминаний по задачам, документам, ЖКХ, налогам, закон-апдейтам и email-demo;
  - генератор уведомлений теперь учитывает пользовательские переключатели и разные горизонты напоминаний;
  - в профиле добавлена карточка «Напоминания»;
  - настройки `Крупный шрифт` и `Высокий контраст` начали применяться на уровне темы Flet.
- Главный дашборд плотнее связан с ЖКХ и налогами:
  - `src/services/dashboard.py` собирает ближайшие и просроченные события из `utility_payments` и `tax_obligations`;
  - `HomePage` показывает карточку «ЖКХ и налоги» с переходами в локальные трекеры;
  - общий счётчик сроков на главной теперь учитывает задачи, ЖКХ и налоги.

## Сейчас в работе

- Текущий мобильный Flet MVP доведен до состояния первой демонстрационной беты на локальных данных.
- Выполнен кроссплатформенный UI-проход и устранены критичные ошибки переключения вкладок.
- Реализована основа базы данных и API для хранения жизненных сценариев.
- Реализована расширенная CRUD-интеграция админ-экрана с API:
  - проблемы/сценарии;
  - этапы/шаги;
  - привязка документов к шагам.
- Реализована первая мобильная интеграция с public API (точечно для сценария «Рождение ребёнка»).
- Реализован интерактивный слой пользовательских действий на локальном состоянии Flet с сохранением между перезапусками, базовым CRUD, созданием ситуаций из шаблонов и главным дашбордом.
- Выполнена отдельная iPhone-адаптация интерфейса без изменения бизнес-логики и локального хранения.
- Продолжается аккуратная полировка пользовательского сценарного слоя: каталог сценариев, детали ситуации, документы из задач.
- Улучшается ручное ведение задач внутри ситуации: сроки и документы становятся пригодными для dashboard-напоминаний, подготовки и уведомлений.
- Для демонстрации руководителю готов основной путь: Главная → Жизненные сценарии → Рождение ребёнка → Создать ситуацию → Мои ситуации → Задачи → Прогресс → Профиль/Обучение → Админ-панель.
- Для первого запуска добавлен onboarding; после кнопки «Начать» он больше не показывается.
- После First Beta утверждён roadmap полного закрытия ТЗ.
- Первый спринт roadmap по личным документам выполнен на уровне локального MVP; production-шифрование остаётся backend/security этапом.
- Второй спринт roadmap по зависимостям шагов выполнен на уровне локального MVP.
- Третий спринт roadmap по госучреждениям выполнен на уровне локального MVP.
- Четвёртый спринт roadmap по ролям и админ-панели начат: добавлен локальный каркас ролей и демонстрационный аудит.
- В этом же спринте локальный CRUD закон-апдейтов доведён до создания, редактирования, публикации, удаления, связи со сценариями, связи с проблемами и демонстрационного аудита.
- Добавлен локальный CRUD справочника официальных источников для редактора контента.
- По приоритету 1 добавлены управляемые напоминания пользователя и блок ЖКХ/налогов на главной; следующий логичный шаг — продолжить CRUD админки для этапов, шагов, учреждений и справочников.
- Начат Sprint 2 по пользовательской логике и ролям:
  - добавлен локальный intent-router встроенного ИИ-помощника;
  - ответы помощника теперь содержат карточку перехода в подходящий раздел;
  - помощник учитывает гостевой режим и предупреждает, если действие требует входа;
  - на desktop-версии главной страницы добавлена плавающая кнопка открытия помощника;
  - глобальный поиск получил Google-like подсказки из проблем, сценариев, документов, учреждений и закон-апдейтов.
- Доработан сценарий показа ролей и гостевого режима:
  - меню пользователя теперь открывает `/settings` через пункт «Настройки»;
  - гостевой режим доступен из быстрого входа и позволяет просматривать основные разделы без аккаунта;
  - добавлен пункт «Выйти в гостевой режим»;
  - добавлен пункт «Выйти со всех аккаунтов» для очистки добавленных быстрых пользователей;
  - «Добавить пользователя» открывает регистрацию даже из активного аккаунта;
  - новый пользователь после регистрации добавляется в быстрый список и сразу выбирается;
  - быстрый вход поддерживает гостя, гражданина, редактора, администратора и локально добавленных пользователей.

## Что нужно сделать следующим

- Продолжить перенос личного слоя React/Vite на backend: профиль, настройки, уведомления и полноценная session-логика после уже подключённых документов, ситуаций и задач.
- После появления Figma-макетов сверить отступы, типографику и состояния с макетом.
- Подготовить скриншоты для дипломной документации.
- Расширять набор шаблонов следующими сценариями: пособия, алименты, покупка жилья, регистрация автомобиля.
- Продолжить спринт roadmap: довести админ-панель редактора/админа площадки до CRUD зависимостей, связанных сценариев, учреждений и справочников.
- Добавить в админ-панель редактирование/удаление этапов и шагов (сейчас есть создание и переключение обязательности).
- Подключить в админке CRUD-формы для зависимостей и related scenarios к финальному UX конструктора (модели и API уже есть).
- Расширить мобильную API-интеграцию с одного сценария на каталог проблем/детали/документы/уведомления.
- Пройти ручную проверку на реальном iPhone по `docs/IOS_LAYOUT_CHECKLIST.md`, особенно на устройствах с Dynamic Island.
- Пройти первую демонстрацию с руководителем и зафиксировать замечания в `docs/TASKS.md`.
- Продолжить спринт roadmap: довести админ-панель редактора/админа площадки до CRUD зависимостей, этапов, шагов, учреждений и справочников.
- Следующим маленьким шагом улучшить редактирование/удаление этапов и шагов или добавить CRUD учреждений в админ-панели.
- Позже перенести хранение личных документов на backend с шифрованием, RBAC и аудитом.
- Позже вынести управление зависимостями шагов в админ-панель.
- Позже добавить CRUD госучреждений в админ-панель и заменить демонстрационные адреса проверенными официальными данными.
- Продолжить Sprint 2: локализация RU/EN/BE, расширенная страница настроек и адаптивная проверка desktop/tablet/mobile.
- Ручно проверить в web-версии сценарий: гость → гражданин → редактор → администратор → добавленный пользователь → выход в гостевой режим → выход со всех аккаунтов.
- По React/Vite-направлению: перейти к авторизации, ролям, гостевому режиму и protected actions.

## Известные проблемы

- Глобальная команда `flet` может быть недоступна без активации `.venv`.
- Команды `python` и `pip` в системе могут отсутствовать; использовать `python3`, `pip3` или команды из `.venv`.
- Поиск и фильтрация реализованы только на моковых данных.
- Авторизация реализована как локальный MVP без реальной безопасности, JWT и RBAC.
- Для работы админ-функций Flet-экрана backend API должен быть запущен отдельно (`uvicorn backend.app:app ...`).
- Обучающий модуль реализован как демо/MVP на локальном состоянии без backend.
- Backend добавлен как MVP-основа, Flet-клиент переведен на API только частично; React/Vite frontend-кандидат уже читает публичные проблемы и сценарии через API с fallback.
- В конструкторе сценариев пока нет drag-and-drop сортировки; порядок задается числом `order_index`.
- В админке пока нет удаления сущностей и массовых операций.
- Локальные пользовательские данные сохраняются в `data/app_state.json`, но это не заменяет backend-хранилище и реальную безопасность.
- Личные документы в демо-версии хранятся локально без шифрования; это допустимо для MVP, но не для production.
- На некоторых платформах Flet может не отдать локальный путь выбранного файла; в этом случае поле «Файл / скан» можно заполнить вручную для демо.
- Справочник учреждений демонстрационный: адреса и телефоны требуют финальной проверки по официальным источникам перед production.
- Шаблоны сценариев пока моковые; позже их нужно получать из public API сценариев.
- `flet run src/main.py` запускает долгоживущий GUI-процесс; для остановки использовать `Ctrl+C`.
- При запуске `flet run -w ...` важно использовать свободный порт; если порт занят, нужно выбрать другой.
- Android-адаптация и планшетные раскладки не выполнялись в текущем iPhone-этапе.
- На реальном iPhone нужно дополнительно проверить поведение клавиатуры в диалогах редактирования документов, ситуаций и задач.

## Команды запуска

С активацией окружения:

```bash
source .venv/bin/activate
flet run src/main.py
```

Без активации окружения:

```bash
.venv/bin/flet run src/main.py
```

Запуск в браузере для проверки внешнего вида:

```bash
.venv/bin/flet run -w src/main.py --host 127.0.0.1 -p 8550
```

Запуск на подключенное iOS-устройство, если Flet CLI и устройство уже настроены:

```bash
.venv/bin/flet run --ios src/main.py
```

Инициализация БД сценариев:

```bash
PYTHONPATH=src python -m backend.scripts.init_db
PYTHONPATH=src python -m backend.scripts.seed_db
```

Запуск API:

```bash
PYTHONPATH=src uvicorn backend.app:app --host 127.0.0.1 --port 8060
```

## Важные ограничения

- Не переписывать backend-стек (FastAPI + SQLAlchemy + SQLite MVP) без отдельной задачи.
- Не добавлять полноценную авторизацию и RBAC без отдельной задачи.
- Мобильный UI пока в основном работает на моковых данных; API подключается поэтапно.
- Все пользовательские тексты интерфейса писать на русском языке.
- После крупных изменений обновлять этот файл.
- При задачах по миграции UI читать `docs/REACT_MIGRATION_AUDIT.md` и `docs/REACT_MIGRATION_PLAN.md`.
