-- 0007_noop.sql: восполнение пропуска в нумерации миграций.
-- Сознательно пустой файл — никаких изменений схемы.
-- Этот файл нужен, чтобы:
--   1) список миграций оставался монотонным (0001..0018);
--   2) внешние инструменты, итерирующие по диапазону, не пропускали шаг.
SELECT 1;
