# Flet = native WebView shell only (src/mobile_webview.py loads the React app).
# flet-web dropped: WebView does not run in Flutter Web.
flet==0.85.2
flet-cli==0.85.2
flet-desktop==0.85.2
flet-webview==0.85.2

# Backend stack
fastapi==0.116.1
uvicorn==0.35.0
sqlalchemy==2.0.41
pydantic==2.11.7
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
cryptography>=41.0.0
email-validator>=2.0.0

# MySQL — единственная СУБД проекта.
# PyMySQL — чистый-Python драйвер (без C-зависимостей), поддерживает MySQL 5.7+/8.0+.
# cryptography нужен для caching_sha2_password (auth plugin по умолчанию в MySQL 8).
pymysql==1.1.1
cryptography>=41.0.0

# Тесты:
# pytest>=8.0
# httpx>=0.27
